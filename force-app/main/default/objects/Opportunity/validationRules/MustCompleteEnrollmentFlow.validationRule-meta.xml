<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>MustCompleteEnrollmentFlow</fullName>
    <active>true</active>
    <description>Requires the enrollment flow to be completed before an opportunity is marked as Enrolled</description>
    <errorConditionFormula>AND( 
NOT($Setup.Americord_Settings__c.Bypass_Validation_Rule__c),
NOT(Enrollment_Flow_Complete__c),
ISPICKVAL(StageName,&apos;Enrolled&apos;),
ISCHANGED(StageName)
)</errorConditionFormula>
    <errorMessage>In order to mark an Opportunity as Enrolled, you must first complete the Enrollment Flow. Click the &quot;Enroll Opportunity&quot; button.  This will automatically move the stage to Enrolled.</errorMessage>
</ValidationRule>
